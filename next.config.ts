import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['madesacms.vtexassets.com', 'static.mobly.com.br'], 
  },
};

export default nextConfig;
